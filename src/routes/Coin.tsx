import { Link } from "react-router-dom";

const Coin = (props) => {
    return (
        <div>
            {/* <Link to='/:coinId'>{props.name}</Link > */}
        </div>
    );
};

export default Coin;